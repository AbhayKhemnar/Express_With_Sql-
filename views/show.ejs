<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table{
            border: 2px solid black;
        }
        th,td{
            border: 1px solid black;
        }

        button{
            cursor: pointer;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th>ID</th>
            <th>NAME</th>
            <th>Email</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        
        <% for(user of data){%>
            <tr>
             <td><%=user.id%></td>  
             <td><%=user.Name%></td>  
             <td><%=user.Email%></td>  
             <td>
                <form method="get" action="http://localhost:8080/users/<%=user.id%>/edit">
                    <button>Update UserName</button>
                </form>
             </td>
             <td>
                <form method="get" action="http://localhost:8080/users/<%=user.id%>/delete">
                    <button>DELETE</button>
                </form>
             </td>
             </tr>
        <%}%>
    </table>
    <a href="http://localhost:8080/users/Add">Add user</a>
</body>
</html>